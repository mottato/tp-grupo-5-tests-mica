{{#partial "contenido" }}
    <div class="table-responsive">
        <h3>Comunidades que administras</h3>
        <table class="table table-striped table-hover">
            <tr>
                <th>Nombre</th>
                <th>Cant. Miembros</th>
                <th>Confiabilidad</th>
                <th>Gestionar Miembros</th>
            </tr>
            <tbody>
            {{#each comunidades}}
                <tr>
                    <td>{{this.nombre}}</td>
                    <td>{{this.cantMiembros}}</td>
                    <td>{{this.confiabilidad}}</td>
                    <td>
                        <a class="btn btn-secondary" href="/comunidades/{{this.id}}/gestionar"><i class="fa-solid fa-pen-to-square"></i></a>
                        <a class="btn btn-secondary" href="/comunidades/editar/{{this.id}}">Editar nombre</a>

                    </td>
                </tr>
            {{/each}}
            </tbody>
        </table>

    </div>
{{/partial}}
{{> templates/base }}